# دليل الإعداد والاستخدام - Argento Store (النظام المبسط)

## 📋 نظرة عامة

تم إنشاء نسخة مبسطة من نظام Argento Store تركز على:
- ✅ **توليد صفحات هبوط لكل منتج**
- ✅ **كتابة روابط صفحات الهبوط في الكتالوج تلقائياً**
- ✅ **نظام ترجمة ذكي للمدن والمناطق**
- ✅ **داشبورد مبسط لعرض المنتجات والطلبات**

---

## 📁 الملفات الجديدة المُنشأة

### 1. **Backend (Python/Flask)**
```
app_simplified.py                              # التطبيق المبسط الأساسي
```

### 2. **Frontend (HTML/CSS/JS)**
```
landing/index_simplified.html                   # صفحة الهبوط المبسطة
landing/script_simplified.js                    # JavaScript مع ترجمة ذكية
templates/admin/dashboard_simplified.html       # داشبورد مبسط
```

### 3. **ملفات التوثيق**
```
SETUP_INSTRUCTIONS.md                           # هذا الملف - دليل الاستخدام
```

---

## 🔧 خطوات التشغيل

### الخطوة 1: النسخ الاحتياطي (اختياري)
```bash
# إنشاء نسخة احتياطية من app.py الأصلي
cp app.py app_old.py
```

### الخطوة 2: استبدال الملفات

#### اختيار أ: استخدام النظام المبسط
```bash
# استبدال app.py بالنسخة المبسطة
cp app_simplified.py app.py

# استبدال صفحة الهبوط
cp landing/index_simplified.html landing/index.html
cp landing/script_simplified.js landing/script.js

# استبدال الداشبورد
cp templates/admin/dashboard_simplified.html templates/admin/dashboard.html
```

#### اختيار ب: استخدام الملفات الجديدة مباشرة
يمكنك أيضاً تعديل `app.py` ليشير إلى الملفات المبسطة بدون استبدال:
- قم بتحديث المسارات في `app.py` لتشير إلى `*_simplified.html/js`

### الخطوة 3: تشغيل التطبيق
```bash
# تشغيل التطبيق المبسط
python app_simplified.py
# أو
python app.py  # إذا قمت بالاستبدال
```

---

## 🌟 المميزات الجديدة

### 1. توليد روابط صفحات الهبوط تلقائياً

#### كيفية العمل:
- عند تحديث الكتالوج من `/admin/update-catalog`، يتم تلقائياً:
  - جلب جميع المنتجات من الكتالوجات
  - إنشاء رابط صفحة هبوط لكل منتج: `https://speedafargento.com/product/{product_id}`
  - حفظ الرابط في حقل `website` لكل منتج في الكتالوج

#### API Endpoints الجديدة:
```
POST /admin/update-product-links
- تحديث روابط صفحات الهبوط لجميع المنتجات
- يتم استدعاؤه تلقائياً بعد تحديث الكتالوج
```

#### مثال:
```json
{
  "id": "SUDIID_001",
  "name": "سكر أبيض",
  "price": 25,
  "website": "https://speedafargento.com/product/SUDIID_001",
  "landing_url": "/product/SUDIID_001"
}
```

### 2. صفحة هبوط مبسطة مع ترجمة ذكية

#### الميزات:
- ✅ **قراءة المدن والمناطق من `addresses.xlsx`**
- ✅ **ترجمة ذكية محلية** بدون الحاجة لـ API خارجي
- ✅ **قاموس ترجمة شامل** للمدن والمناطق الشائعة
- ✅ **واجهة نظيفة وسهلة الاستخدام**

#### كيفية عمل الترجمة:
```javascript
// قاموس ترجمة مدمج في JavaScript
const cityTranslations = {
    'Sharqia': 'الشرقية',
    'Cairo': 'القاهرة',
    'Giza': 'الجيزة',
    // ... إلخ
};

// دالة ترجمة ذكية
function translateToArabic(text) {
    // تفحص إذا كان النص عربي أصلاً
    if (/[\u0600-\u06FF]/.test(text)) {
        return text;
    }
    
    // تبحث في القاموس
    return cityTranslations[text] || text;
}
```

### 3. داشبورد محسّن

#### الميزات:
- ✅ **عرض جميع المنتجات مع الصور**
- ✅ **نسخ روابط صفحات الهبوط بضغطة زر**
- ✅ **عرض الطلبات الأخيرة**
- ✅ **إحصائيات مباشرة**
- ✅ **تحديث الكتالوج والروابط من الواجهة**

---

## 📡 API Endpoints الكاملة

### المنتجات
```
GET  /api/products                 # جلب جميع المنتجات (مع الروابط)
GET  /api/product/<id>             # جلب منتج محدد
POST /admin/update-product-links   # تحديث روابط المنتجات
POST /admin/update-catalog         # تحديث الكتالوج + الروابط
```

### الطلبات
```
GET  /api/orders                   # جلب جميع الطلبات
POST /api/order                    # إنشاء طلب جديد
```

### اللوحة
```
GET  /admin                        # الداشبورد
GET  /api/dashboard-stats          # الإحصائيات
GET  /admin/export-speedaf         # تصدير Speedaf
```

### صفحات الهبوط
```
GET  /product/<id>                 # صفحة هبوط المنتج
```

---

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: إضافة منتجات جديدة

1. **تحديث الكتالوج من الداشبورد**
   ```
   افتح: /admin
   اضغط على: "تحديث الكتالوج"
   ```

2. **تُنشأ الروابط تلقائياً**
   - كل منتج سيحصل على رابط: `/product/{id}`
   - يُحفظ في حقل `website` تلقائياً

3. **استخدام الروابط**
   - انسخ الرابط من الداشبورد
   - شارك الرابط مع العملاء
   - يفتح العميل صفحة الهبوط ويطلب المنتج

### السيناريو 2: استقبال طلب جديد

1. **العميل يفتح صفحة الهبوط**
   ```
   https://speedafargento.com/product/SUDIID_001
   ```

2. **العميل يملأ النموذج**
   - الاسم، الهاتف
   - المحافظة (تُعرض بالعربية تلقائياً)
   - المنطقة (تُعرض بالعربية تلقائياً)
   - العنوان التفصيلي

3. **الطلب يُحفظ في `data/orders.json`**
   - بصيغة Speedaf الجاهزة للتصدير

### السيناريو 3: تصدير الطلبات لـ Speedaf

1. **افتح الداشبورد**
   ```
   /admin
   ```

2. **اضغط "تصدير Speedaf"**
   - يتم توليد ملف CSV
   - تنزيل الملف تلقائياً

3. **ارفع الملف على Speedaf**
   - الملف جاهز بالتنسيق الصحيح (22 عمود)

---

## 🗂️ هيكل البيانات

### كتالوج المنتجات (`data/catalog_cache.json`)
```json
{
  "metadata": {
    "total_products": 10,
    "last_updated": "2024-12-07T20:00:00",
    "links_updated_at": "2024-12-07T20:00:00"
  },
  "products": [
    {
      "id": "SUDIID_001",
      "name": "سكر أبيض",
      "title": "سكر أبيض ناعم 1 كجم",
      "price": 25,
      "merchant_id": "SUDIID",
      "merchant_name": "Azúcar",
      "image": "https://...",
      "website": "https://speedafargento.com/product/SUDIID_001",
      "landing_url": "/product/SUDIID_001"
    }
  ]
}
```

### الطلبات (`data/orders.json`)
```json
[
  {
    "order_id": "ORD-20241207-ABC123",
    "product_id": "SUDIID_001",
    "product_title": "سكر أبيض ناعم 1 كجم",
    "product_price": 25,
    "merchant_id": "SUDIID",
    "merchant_name": "Azúcar",
    "customer_name": "أحمد محمد",
    "customer_phone": "01012345678",
    "shipping_city": "Sharqia",
    "shipping_area": "Zagazig",
    "shipping_address": "شارع النور، حي الزهور",
    "shipping_cost": 50,
    "total_amount": 75,
    "status": "pending",
    "created_at": "2024-12-07T20:30:00"
  }
]
```

---

## 🔍 استكشاف الأخطاء

### المشكلة 1: المنتجات لا تظهر

**الحل:**
```bash
# تحقق من وجود الكتالوج
ls -lh data/catalog_cache.json

# إذا لم يوجد، قم بتحديثه
curl -X POST http://localhost:10000/admin/update-catalog
```

### المشكلة 2: الروابط لا تعمل

**الحل:**
```bash
# تحديث الروابط يدوياً
curl -X POST http://localhost:10000/admin/update-product-links
```

### المشكلة 3: المدن لا تُعرض بالعربية

**الحل:**
- تحقق من وجود قاموس الترجمة في `script_simplified.js`
- تأكد من تحميل ملف Excel بنجاح
- افتح Console في المتصفح وتحقق من الأخطاء

### المشكلة 4: ملف Excel لا يُحمّل

**الحل:**
```javascript
// في script_simplified.js، غيّر رابط Excel:
const excelUrl = 'https://raw.githubusercontent.com/YOUR_REPO/main/data/addresses.xlsx';
```

---

## 📊 مقارنة بين النظام القديم والمبسط

| الميزة | النظام القديم | النظام المبسط |
|-------|---------------|---------------|
| صفحات الهبوط | يدوية | تلقائية |
| روابط المنتجات | يدوية | تُحفظ تلقائياً في الكتالوج |
| ترجمة المدن | تتطلب API | محلية بقاموس مدمج |
| عرض المنتجات | معقد | مبسط ومباشر |
| تحديث الروابط | يدوي | زر واحد من الداشبورد |
| الأداء | متوسط | أسرع وأخف |

---

## ✅ قائمة التحقق النهائية

- [ ] قمت بنسخ احتياطي من الملفات الأصلية
- [ ] استبدلت الملفات بالنسخ المبسطة
- [ ] شغّلت التطبيق بنجاح
- [ ] حدّثت الكتالوج من الداشبورد
- [ ] تأكدت من ظهور روابط صفحات الهبوط
- [ ] اختبرت صفحة هبوط واحدة على الأقل
- [ ] تأكدت من ترجمة المدن والمناطق للعربية
- [ ] أنشأت طلب تجريبي بنجاح
- [ ] صدّرت الطلبات بصيغة Speedaf

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع قسم "استكشاف الأخطاء" أعلاه
2. تحقق من السجلات (Logs):
   ```bash
   # في Terminal حيث يعمل التطبيق
   # ستظهر رسائل خطأ واضحة
   ```
3. تأكد من المتطلبات:
   ```bash
   pip install -r requirements.txt
   ```

---

## 🎉 ملاحظات نهائية

### الملفات التي يجب استبدالها:
```
✅ app.py               ← app_simplified.py
✅ landing/index.html   ← landing/index_simplified.html
✅ landing/script.js    ← landing/script_simplified.js
✅ templates/admin/dashboard.html ← templates/admin/dashboard_simplified.html
```

### الملفات التي تبقى كما هي:
```
⚪ config.py            # يحتوي قوائم المدن الكبيرة (احتياطية)
⚪ utils/*.py           # جميع الأدوات المساعدة
⚪ data/addresses.xlsx  # ملف Excel للمدن
⚪ landing/style.css    # التنسيقات
```

---

**تم الإنشاء في:** 2024-12-07  
**الإصدار:** 2.0 - النظام المبسط  
**المطور:** GenSpark AI Assistant
