# 🛍️ Argento Store - النظام المبسط

> نظام متكامل لإدارة الطلبات مع صفحات هبوط تلقائية لكل منتج

## ✨ المميزات الرئيسية

- ✅ **توليد صفحات هبوط تلقائياً** لكل منتج مع رابط فريد
- ✅ **حفظ الروابط في الكتالوج** تلقائياً في حقل `website`
- ✅ **ترجمة ذكية محلية** للمدن والمناطق (بدون API خارجي)
- ✅ **قراءة المدن من Excel** مباشرة مع معالجة ذكية
- ✅ **داشبورد محسّن** مع نسخ الروابط بضغطة واحدة
- ✅ **تصدير Speedaf جاهز** (22 عمود)

---

## 🚀 التشغيل السريع

### 1. استبدال الملفات
```bash
# Backend
cp app_simplified.py app.py

# Frontend
cp landing/index_simplified.html landing/index.html
cp landing/script_simplified.js landing/script.js

# Dashboard
cp templates/admin/dashboard_simplified.html templates/admin/dashboard.html
```

### 2. التشغيل
```bash
python app.py
```

### 3. افتح المتصفح
```
http://localhost:10000/admin
```

---

## 📂 الملفات الجديدة

| الملف | الوصف |
|------|-------|
| **app_simplified.py** | التطبيق الأساسي المبسط |
| **landing/index_simplified.html** | صفحة الهبوط المحسّنة |
| **landing/script_simplified.js** | JavaScript مع ترجمة ذكية |
| **templates/admin/dashboard_simplified.html** | لوحة تحكم حديثة |
| **SETUP_INSTRUCTIONS.md** | دليل مفصل (اقرأه!) |
| **FILES_SUMMARY_AR.md** | ملخص التعديلات |

---

## 🎯 كيف يعمل النظام؟

### 1. تحديث الكتالوج
```
الداشبورد → "تحديث الكتالوج" → جلب المنتجات من فيسبوك
```
**النتيجة:** يُنشأ رابط لكل منتج تلقائياً

### 2. مشاركة الروابط
```
الداشبورد → نسخ رابط المنتج → مشاركة مع العملاء
```
**مثال:** `https://speedafargento.com/product/SUDIID_001`

### 3. استقبال الطلبات
```
العميل → صفحة الهبوط → تعبئة النموذج → إرسال
```
**النتيجة:** طلب جديد في `data/orders.json`

### 4. تصدير Speedaf
```
الداشبورد → "تصدير Speedaf" → تنزيل CSV
```
**النتيجة:** ملف جاهز بـ 22 عمود

---

## 🔗 API Endpoints

### المنتجات
- `GET /api/products` - جلب جميع المنتجات (مع روابط)
- `GET /api/product/<id>` - جلب منتج محدد
- `POST /admin/update-product-links` - تحديث الروابط

### الطلبات
- `GET /api/orders` - جلب جميع الطلبات
- `POST /api/order` - إنشاء طلب جديد

### الإدارة
- `GET /admin` - لوحة التحكم
- `POST /admin/update-catalog` - تحديث الكتالوج
- `GET /admin/export-speedaf` - تصدير CSV

---

## 🌍 الترجمة الذكية

### كيف تعمل؟
```javascript
// قاموس مدمج محلياً
const cityTranslations = {
    'Sharqia': 'الشرقية',
    'Cairo': 'القاهرة',
    'Giza': 'الجيزة'
    // ... 30+ مدينة
};

// ترجمة تلقائية
translateToArabic('Cairo') // → 'القاهرة'
```

### لا حاجة لـ:
- ❌ API خارجي
- ❌ مكتبة ترجمة
- ❌ اتصال بالإنترنت بعد تحميل الصفحة

---

## 📊 مثال على البيانات

### الكتالوج (`data/catalog_cache.json`)
```json
{
  "products": [
    {
      "id": "SUDIID_001",
      "name": "سكر أبيض",
      "price": 25,
      "website": "https://speedafargento.com/product/SUDIID_001"
    }
  ]
}
```

### الطلب (`data/orders.json`)
```json
[
  {
    "order_id": "ORD-20241207-ABC123",
    "product_title": "سكر أبيض",
    "customer_name": "أحمد محمد",
    "shipping_city": "Sharqia",
    "total_amount": 75
  }
]
```

---

## ✅ خريطة الطريق

- [x] توليد روابط صفحات الهبوط
- [x] حفظ الروابط في الكتالوج
- [x] ترجمة ذكية محلية
- [x] داشبورد محسّن
- [x] قراءة المدن من Excel
- [x] دليل استخدام شامل

---

## 📞 الدعم

**وثائق كاملة:** اقرأ `SETUP_INSTRUCTIONS.md`  
**ملخص سريع:** اقرأ `FILES_SUMMARY_AR.md`

---

## 🎉 ملاحظات

- ⚡ **سريع:** لا يعتمد على APIs خارجية
- 🔒 **آمن:** الترجمة محلية بالكامل
- 🧹 **بسيط:** 4 ملفات فقط للاستبدال
- 📱 **متجاوب:** يعمل على جميع الأجهزة

---

**الإصدار:** 2.0 - النظام المبسط  
**التاريخ:** 2024-12-07  
**الحالة:** ✅ جاهز للاستخدام
